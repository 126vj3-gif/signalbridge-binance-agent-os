Track B submission: SignalBridge for Binance Agent OS.

It scans the USDT perpetual universe, generates a structured altcoin futures signal, re-checks market state through Binance MCP, and applies local controls for stale prices, stop width, leverage, duplicate positions, cooldown and daily loss before producing an execution preview. The repository includes a reproducible demo and an audit trail. This submission build defaults to paper mode so the demo never misrepresents a simulated fill as a live Binance order.

Demo: [add public video URL]
GitHub: [add public repository URL]
